<?xml version="1.0" encoding="utf-8" ?>
<toast:ToastPageBase
    x:Class="DeskBorder.Pages.Toast.WarningToastPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toast="using:DeskBorder.Pages.Toast"
    mc:Ignorable="d">
    <Grid Background="Transparent">
        <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid RowSpacing="12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    FontSize="18"
                    FontWeight="SemiBold"
                    Text="{x:Bind PresentationOptions.Title}"
                    TextWrapping="NoWrap" />

                <TextBlock
                    Grid.Row="1"
                    Text="{x:Bind PresentationOptions.Message}"
                    TextWrapping="WrapWholeWords" />

                <TextBlock
                    Grid.Row="2"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind PresentationOptions.ActionCardTitle, Mode=OneWay}"
                    Visibility="{x:Bind GetActionCardTitleVisibility(PresentationOptions.ActionCardTitle), Mode=OneWay}" />

                <Button
                    Grid.Row="3"
                    HorizontalAlignment="Stretch"
                    Click="OnActionButtonClicked"
                    Content="{x:Bind PresentationOptions.ActionButtonText, Mode=OneWay}" />
            </Grid>
        </Border>
    </Grid>
</toast:ToastPageBase>
